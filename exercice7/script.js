// Création du tableau de noms
let ajout = [''];

    document.write(ajouut);
let clickbutton = document.querySelector('#btnarticle');

clickbutton.addEventListener('click', () => {
  let articles = prompt("ajouter un article :");
  if (articles) {
      alert("articles ajouter");
    } else {
      alert("ajouter l'article");
    }

  }
);